---
title: Status
description: A flexible component for displaying status indicators or messages.
---
import StatusPreview from "@/components/StatusPreview.svelte"
import { Tabs, TabItem } from '@astrojs/starlight/components'

<Tabs>
  <TabItem label="Preview">
    <StatusPreview />
  </TabItem>
  <TabItem label="Usage">
    ```svelte
    <script lang="ts">
      import { Status } from "@dxdns/feflow"

      const variants = ["pulse", "ping", "bounce"]
      const colors = ["blue", "red", "green"]
    </script>

    <div style="max-width: 300px; margin: 0 auto; line-height: 3;">
      {#each variants as variant, i}
        <Status variant={variant as any} color={colors[i]} />
        <br />
      {/each}

      {#each ["5rem", 100, "150px", 200] as size, i}
        <Status
          color={i % 2 ? "aqua" : "orange"}
          variant={i % 2 ? "bounce" : "none"}
          {size}
        />
        <br />
      {/each}
    </div>
    ```
  </TabItem>
</Tabs>